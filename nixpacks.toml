# Nixpacks Configuration for VideoPopup Frontend
# Forces Node.js detection and build process

[phases.setup]
nixPkgs = ["nodejs"]

[phases.install]
cmds = ["npm install"]

[phases.build]
dependsOn = ["install"]
cmds = ["npm run build"]

# Static site - no start command needed (served by nginx)

